/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import HomePg from './src/pages/HomePg';

AppRegistry.registerComponent(appName, () => HomePg);
